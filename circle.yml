machine:
  ruby:
    version: 2.1.2
  node:
    version: 4
dependencies:
  pre:
    - echo //registry.npmjs.org/:_authToken=${NPM_TOKEN} > .npmrc
general:
  artifacts:
    - coverage/**/*
    - log/*.log
    - npm-debug.log
