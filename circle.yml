machine:
  ruby:
    version: 2.1.2
  node:
    version: v0.10.31
deployment:
  production:
    branch: master
    commands:
      - bundle install
      - bundle exec cap production deploy
general:
  artifacts:
    - coverage/**/*
    - log/*.log
    - npm-debug.log
